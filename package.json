{
	"name": "dreamtui",
	"version": "0.1.1",
	"description": "A terminal-based generative dream engine — transforms user-written dreams into full-screen, animated ASCII experiences.",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run index.ts",
		"dev": "bun run --watch index.ts",
		"check": "biome check --write",
		"build": "bun build ./index.ts --outdir ./dist --target=bun"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.7",
		"@types/bun": "^1.3.10"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"@opentui/core": "^0.1.87"
	}
}
